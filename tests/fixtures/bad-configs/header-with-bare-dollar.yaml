services:
  - name: test
    listen: 11436
    upstream: http://mock-upstream:8080
    timeout: 30s
    headers:
      # A bare $ after expansion should be rejected
      X-Custom: "value with $bare dollar"
