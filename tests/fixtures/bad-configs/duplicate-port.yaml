services:
  - name: service-a
    listen: 11436
    upstream: http://mock-upstream:8080
    timeout: 30s
    headers:
      Authorization: "Bearer ${TEST_KEY}"
  - name: service-b
    listen: 11436
    upstream: http://mock-upstream:8080
    timeout: 30s
    headers:
      Authorization: "Bearer ${TEST_KEY}"
